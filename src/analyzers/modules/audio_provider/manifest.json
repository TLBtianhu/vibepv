{
  "name": "audio_provider",
  "description": "音频文件提供器，将音频拷贝到渲染器可读目录",
  "produces": {
    "audio_file": {
      "file": "src/renderer/public/{audio_file}",
      "field": "audio_file",
      "required_by": ["AudioPlayer", "CircularSpectrum", "LyricsVideo"]
    }
  }
}